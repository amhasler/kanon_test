= form_for(@artobject)  do |f|
  = render 'shared/error_messages', object: f.object
  .field
    = f.text_field :name, placeholder: "Name the art object"
  = f.submit "Create", class: "btn btn-large btn-primary"