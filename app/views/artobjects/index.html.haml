- provide(:title, "Art objects")

.row
  %aside.span12
    - if signed_in?
      = render 'shared/artobject_form'

.row
  .span12
    %h3 Art objects (#{ @artobjects.count })
    %ol.artobjects
      = render @artobjects

    = will_paginate